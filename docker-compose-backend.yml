version: '3.8'

services:
  # Backend Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: lablogbook-backend
    environment:
      - SPRING_PROFILES_ACTIVE=production
      - SPRING_DATASOURCE_URL=jdbc:mysql://gondola.proxy.rlwy.net:24482/railway?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=clCBnwidlucjnLmydCKnaqmfQDLZTBzO
      - JWT_SECRET=your-secret-key-here-make-it-long-and-secure-for-production
      - JWT_EXPIRATION=86400000
      - SPRING_WEB_CORS_ALLOWED_ORIGINS=http://localhost:3000
      - SERVER_PORT=8080
    ports:
      - "8080:8080"
    volumes:
      - ./backend/uploads:/app/uploads 